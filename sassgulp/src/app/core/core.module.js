(function () {
    'use strict';

    angular.module('app.core', [
        'ngMessages',
        'ui.router'
    ])
        .config(config);

    config.$inject = [];

    function config() {

    }

})();
